<Window x:Class="StudentManagementApp.Views.AddSubjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm Môn Học Mới ($push và Gợi ý)"
        Height="470" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI"
        Loaded="Window_Loaded">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="📚 Thêm Môn Học Mới" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Gõ mã môn để xem gợi ý tự động, hoặc nhập môn học mới:" FontSize="12.5" Foreground="#64748B" Margin="0,0,0,14"/>

        <!-- Form nhập liệu -->
        <StackPanel Grid.Row="2">
            <!-- Mã môn + Gợi ý -->
            <TextBlock Text="Mã môn học (*): (Ví dụ: cs, lap, web...)" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtMaMon" Height="34" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,0,4"
                     TextChanged="TxtMaMon_TextChanged" PreviewKeyDown="TxtMaMon_PreviewKeyDown"/>

            <!-- Khung danh sách gợi ý môn học -->
            <Border x:Name="BrdSuggestions" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="4" 
                    MaxHeight="110" Margin="0,0,0,10" Visibility="Collapsed">
                <ListBox x:Name="LstSuggestions" BorderThickness="0" Background="Transparent" 
                         SelectionChanged="LstSuggestions_SelectionChanged" DisplayMemberPath="DisplayText"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" Cursor="Hand"/>
            </Border>

            <!-- Tên môn -->
            <TextBlock Text="Tên môn học (*): (Tự động điền khi chọn gợi ý)" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtTenMon" Height="34" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,0,12"/>

            <!-- Điểm số -->
            <TextBlock Text="Điểm số (Thang điểm 0.0 - 10.0) (*):" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtDiem" Height="34" VerticalContentAlignment="Center" Padding="8,0" Text="8.0"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Hủy" Width="80" Height="34" Background="#E2E8F0" Foreground="#334155"
                    BorderThickness="0" Margin="0,0,10,0" Click="BtnCancel_Click" Cursor="Hand"/>
            <Button Content="Thêm vào CSDL ($push)" Width="180" Height="34" Background="#10B981" Foreground="White"
                    FontWeight="SemiBold" BorderThickness="0" Click="BtnOk_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
